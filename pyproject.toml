[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "customs-ai-checker"
version = "0.1.0"
description = "Customs AI Checker foundation"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn>=0.30",
    "pydantic>=2.8",
    "pydantic-settings>=2.4",
    "PyYAML>=6.0",
    "python-multipart>=0.0.9",
    "pypdf>=5,<7",
    "openpyxl>=3.1,<4",
    "xlrd>=2.0.1,<3",
]

[project.optional-dependencies]
vision = [
    "httpx>=0.27,<1",
    "numpy>=2.1,<3",
    "Pillow>=10,<13",
    "pypdfium2>=5,<6",
    "rapidocr>=3.9,<4",
    "onnxruntime>=1.29,<2",
]
test = [
    "pytest>=8.0",
    "httpx>=0.27,<1",
    "numpy>=2.1,<3",
    "Pillow>=10,<13",
    "pypdfium2>=5,<6",
    "xlwt==1.3.0",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
